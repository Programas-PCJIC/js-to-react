const nombre = "John";
const apellido = "Florez";

console.log(nombre + " " + apellido)
console.log(`${nombre} ${apellidp}`)

//En react
function Componente( { backgroundColor }){
    return(<div className={`bg-color-${backgroundColor}`> Hola </div>})
}
